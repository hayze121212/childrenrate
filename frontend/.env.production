# Продакшн-сборка (npm run build): фронтенд обращается к локальному backend CodeIgniter.
# Аналог Config Vars на Heroku из методички (гл. 6), но для localhost.
VUE_APP_BACKEND_URL=http://localhost:8080
